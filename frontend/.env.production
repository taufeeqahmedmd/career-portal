# Production build settings. No secrets here - safe to commit.

# Source maps would expose the app's source to anyone who opens devtools, and
# several dependencies ship maps referencing files they do not include, which
# floods the build log with "Failed to parse source map" noise.
GENERATE_SOURCEMAP=false
